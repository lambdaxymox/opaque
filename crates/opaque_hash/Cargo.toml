[package]
name = "opaque_hash"
version = "0.1.0"
edition = "2024"

[features]
default = ["std"]
std = []

[dependencies]

[dev-dependencies]

[dev-dependencies.criterion]
version = "0.6.0"

[[bench]]
name = "bench_hasher"
harness = false
path = "benches/lib.rs"
